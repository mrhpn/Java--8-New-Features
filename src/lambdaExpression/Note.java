package lambdaExpression;

public class Note {
	/**
	 * based on Lambda Calculus (initially used by LISP programming)
	 * 
	 * - anonymous functions
	 * 
	 * SIMPLE Lambda
	 * -------------
	 * (Arguments) -> Statement; // arguments become some statement
	 * -> // means `become`
	 * (str) -> System.out.print(str);
	 * 
	 * BLOCK Lambda
	 * ------------
	 * (str) -> {
	 *  // ---
	 * }
	 * 
	 * 
	 */
}
