package predicates;

public class Note {
	/**
	 * Predicate (Functional Interface) - inside java.util
	 * --------------------------------
	 * interface predicate<T> {
	 * 		boolean test(T t);
	 * 
	 * 		default and() {}
	 * 		default or() {}
	 * 		default not() {}
	 * }
	 */
}
